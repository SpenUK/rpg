

<% if turn.maker == @player1 %>
<div class="battle_turn defense_turn">
	<p> 
		<%= turn.display_message(:maker) %>
	</p>
<% else %>
<div class="battle_turn offense_turn">
	<p>
		<%= turn.display_message(:target) %>
	</p>
<% end %>

</div>



<!-- maker_id: integer, maker_type: string, target_id: integer, target_type: string, damage: integer, healed: integer, skill_used: string, skill_id: integer, skill_type: string, item_used: string, item_used_id: integer, item_used_type: string, created_at: datetime, updated_at: datetime, fight_id: integer, fight_type: string, critical: boolean -->